#ifndef __LED
#define __LED //these lines might have to change?

	extern int read_LEDs_ASM(); // so we can use it in the main c file

	extern void write_LEDs_ASM(int val); 	// so we can use it in the main C file

#endif
